# We can write some code inside the functions and then call the name of function to display what's inside
* So functions let use reuse the code inside many times, we can type functions in the script section

# Now we can use it to put all our code which repeat inside a function pick Computer move
* At the end we can also type return to get value out of these function
* We can return computerMove, because it requires to be outside of the function
* And once we have local variable computerMove, i gonna assign to the variable the function pick move result
* const computerMove = pickComputerMove();

# And the parameter puts a value into a function
* Por example we put parameters into the functions bracket and then we put variable value when we call the function
* To assign default of parameter we type it after parameter (parameter = 1) 
* And now if we don't type the the value once we call the function the default value will be assigned
